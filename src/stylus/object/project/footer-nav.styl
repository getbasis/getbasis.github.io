.p-footer-nav {
  _font-size-line-height(14px);

  &__list {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  &__item {
    margin: 0;
    _padding(0, 0, .5);

    a {
      text-decoration: none;
    }
  }
}
